# Configuración PRODUCCIÓN (Railway)
spring.datasource.url=jdbc:mysql://shinkansen.proxy.rlwy.net:47716/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=fRKSDseRsiaIbcKuflKaZLPAQMLqLwnA

# Puerto asignado por Railway
server.port=${PORT:8080}

# JWT Configuration (production - MUST set JWT_SECRET env var, same as auth-service)
jwt.secret=${JWT_SECRET}
